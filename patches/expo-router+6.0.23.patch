diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 23fff02..7d01cd8 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -66,16 +66,10 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
             return undefined;
         }
         if (enabled !== false && linkingHandlers.length) {
-            console.error([
-                'Looks like you have configured linking in multiple places. This is likely an error since deep links should only be handled in one place to avoid conflicts. Make sure that:',
-                "- You don't have multiple NavigationContainers in the app each with 'linking' enabled",
-                '- Only a single instance of the root component is rendered',
-                react_native_1.Platform.OS === 'android'
-                    ? "- You have set 'android:launchMode=singleTask' in the '<activity />' section of the 'AndroidManifest.xml' file to avoid launching multiple instances"
-                    : '',
-            ]
-                .join('\n')
-                .trim());
+            // When the Android activity is recreated (e.g., launcher HOME intent) while
+            // the JS context survives, stale handlers remain. Clear them to avoid false
+            // "linking in multiple places" errors.
+            linkingHandlers.length = 0;
         }
         const handler = Symbol();
         if (enabled !== false) {
